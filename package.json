{
  "name": "iii-finder",
  "version": "1.0.0",
  "description": "Image in Image finder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist/index",
    "clean": "rm -rf dist",
    "compile": "tsc",
    "dev": "nodemon -e ts --exec \"npm run clean && npm run compile && npm start\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "get-pixels": "^3.3.0"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
